# UFC Fight Prediction — Calibration Report

Generated by `train_report.py`. Seeds fixed (42); strict temporal validation.

**Data:** 8301 UFC fights 2003-02-28 to 2026-08-01,
features computed strictly as-of fight date by replaying career histories
(`build_features.py`). **Split:** train ≤ 2023-06-03 (6676 fights),
test after (1625 fights). LightGBM tuned with 4-fold expanding-window CV
inside the train period (best: num_leaves=15, lr=0.03,
min_child_samples=60, cv logloss 0.6561). Isotonic
calibration and the market blend were fitted on out-of-fold CV predictions only.

## Model comparison — full test set (1625 fights)

| Predictor | n | Accuracy | Log loss | Brier | ECE |
|---|---|---|---|---|---|
| LightGBM | 1625 | 0.628 | 0.650 | 0.229 | 0.045 |
| LightGBM + isotonic | 1625 | 0.631 | 0.668 | 0.231 | 0.039 |
| Logistic regression (full features) | 1625 | 0.605 | 0.652 | 0.230 | 0.057 |
| Elo only (logistic) | 1625 | 0.577 | 0.676 | 0.242 | 0.039 |
| Better record baseline | 1625 | 0.598 | 1.466 | 0.338 | 0.275 | hard picks scored as 0.99/0.5/0.01

## The real benchmark — vig-removed closing odds (1200 test fights with odds)

| Predictor | n | Accuracy | Log loss | Brier | ECE |
|---|---|---|---|---|---|
| Market (no-vig closing odds) | 1200 | 0.702 | 0.582 | 0.198 | 0.030 |
| LightGBM | 1200 | 0.639 | 0.639 | 0.224 | 0.033 |
| LightGBM + isotonic | 1200 | 0.643 | 0.650 | 0.225 | 0.035 |
| Blend: market + model | 1200 | 0.705 | 0.577 | 0.197 | 0.027 | logit blend, model coef=+0.269

**Does the model beat the market?** No. The market's log loss (0.582) beats the model's (0.639).

**Does the model add information beyond the market?** Not conclusively —
blending model with market gives log loss 0.577 vs 0.582 for the
market alone. Paired bootstrap on the log-loss difference (market − blend,
10,000 resamples): 95% CI [-0.0001, +0.0089], blend better in
97.4% of resamples.

Isotonic calibration (fitted on out-of-fold predictions) improved ECE slightly
but worsened log loss, so the raw LightGBM probabilities — already close to
calibrated — are the primary model output.

This is the expected result for public pre-fight data: closing odds aggregate
sharp bettors' information (injuries, camp changes, weight-cut issues, insider
knowledge) that no historical-stats model can see. Matching the market's
calibration while using only public stats is the honest achievement; claiming
to beat closing lines would be the red flag.

## Calibration

![Reliability diagram](plots/reliability.png)

ECE (10 equal-count bins): model 0.033, market 0.030.

## What carries signal

![Permutation importance](plots/importance.png)

Top 10 by permutation importance (mean log-loss increase over 5 shuffles):

- `age_diff`: +0.0351
- `red_corner`: +0.0293
- `sapm_diff`: +0.0042
- `td_avg_diff`: +0.0037
- `str_def_diff`: +0.0031
- `n_fights_diff`: +0.0029
- `elo_diff`: +0.0020
- `str_acc_diff`: +0.0015
- `sapm_mean`: +0.0013
- `kd_taken_p15_diff`: +0.0011

## Caveats

- Fighters are keyed by normalized name; a handful of duplicate names
  (e.g. two Bruno Silvas) introduce minor noise.
- Elo starts at 1500 on UFC debut; pre-UFC records are not observed.
- Odds are closing odds from the Ultimate UFC Dataset (1200/1625
  test fights matched); fights without odds are excluded only from market rows.
- Fight time is approximated with 5-minute rounds (exact for the modern era).
